id: adr-author
name: adr-author
description: "Use when a durable architectural or design decision is made — choosing a library, setting a module boundary rule, picking a persistence or auth approach, or resolving a design debate. Records it as a numbered ADR in docs/adr (or a line in decision-log.md for smaller calls)."
version: 0.1.0

targets:
  agents_md:
    enabled: false
  claude_md:
    enabled: false
  gemini:
    enabled: false
  claude_skill:
    enabled: true
