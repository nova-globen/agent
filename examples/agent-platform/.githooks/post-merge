#!/usr/bin/env bash
set -euo pipefail
repo_root="$(git rev-parse --show-toplevel)"
( cd "${repo_root}" && dotnet agent sync ) >/dev/null 2>&1 \
  || echo "[post-merge] agent sync skipped (run 'dotnet agent sync' manually)"
