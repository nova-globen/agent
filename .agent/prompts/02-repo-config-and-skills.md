# Prompt 02: Config and Skills

Implement parsing and validation for `.agent/agent.yaml`, `.agent/skills/<skill-id>/skill.yaml`, and `.agent/skills/<skill-id>/SKILL.md`. Add tests for valid config, invalid config, missing files, and duplicate skill IDs.
