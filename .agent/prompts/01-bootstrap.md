# Prompt 01: Bootstrap

Read `AGENTS.md`, `CLAUDE.md`, and all files under `.agent/`.

Create the initial .NET solution for Agent Sync. Implement `agent --version`, `agent doctor`, `agent init`, and `agent status`. Add tests. Stop when tests pass.
