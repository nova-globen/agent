Prepare/refresh the public alpha announcement for Agent Sync.

1. Read `README.md` and `docs/PROMOTION.md`.
2. Confirm the README has: badges, a Quick Demo, an alpha/limitations section,
   "Who this is for", and "What Agent Sync is not". Confirm links work and branch
   references use `master`.
3. Update `docs/PROMOTION.md` copy (tagline, short/long posts, demo script, feedback
   wanted) so it is accurate for the current release and clearly marked alpha.
4. Keep the tone confident but honest about alpha status. No private URLs or local paths.
5. Do not change product behavior. No AI/Claude trailers in commits.
